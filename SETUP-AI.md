# kbbs 設置・改変マニュアル（AIエージェント向け）

このファイルは、Claude Code / Codex などのAIエージェントがこの商品を設置・カスタマイズするためのマニュアルです。人間の方は、このフォルダごとAIエージェントに渡して「設置して」と伝えてください。

## これは何か

**kbbs** = 宣伝OK・リンクOKの1ファイルPHP掲示板。
- 会社・お店・サービスの宣伝投稿を歓迎する（「宣伝お断り」の逆張り）
- 投稿されたURLはリンクとして表示される（投稿者にとってバックリンクになる）
- スパム対策の芯: **投稿時のメールアドレスが、宣伝するURLと同じドメインでなければエラー**（`kbbs_domain_ok()`）。その会社の本人しか投稿できない

## 構成

| ファイル | 役割 |
|---|---|
| `kbbs.php` | 本体（これ1つで動く） |
| `kbbs_posts.log.php` | 投稿データ（自動生成・JSONL・先頭行の `<?php exit;` でWeb直読み不可） |

DB不要・Composer不要。PHP 7.4+ が動くレンタルサーバーにFTPで置くだけ。

## 設置手順

1. `kbbs.php` をWebルート（またはお好みのディレクトリ）にアップロード
2. `kbbs.php` 冒頭の `KBBS_ADMIN_KEY` を**必ず推測困難な文字列に変更**する
   （管理画面: `kbbs.php?admin=1&key=<設定した値>`）
3. ブラウザで開けば完了。投稿データファイルは初回投稿時に自動生成される
4. ディレクトリにPHPの書き込み権限が必要（レンタルサーバーは通常OK）

## 2つの動作モード（自動切り替え）

- **スタンドアロンモード**（既定）: 同ディレクトリに `auth_common.php` が無い場合。
  誰でも投稿フォームを開けるが、投稿には同一ドメインメールが必須（本人性チェックは効いたまま）。
  管理画面は `?admin=1&key=KBBS_ADMIN_KEY` 。
- **𝕏ログインモード**: 同ディレクトリに `auth_common.php`（X OAuthブートストラップ。
  `url2ai_auth_login_url()` / `url2ai_auth_logout_url()` / `url2ai_auth_bootstrap()` を提供する実装）を置くと、
  投稿に𝕏ログインが必須になり、投稿者の@ハンドルが表示される。管理判定も認証側の `is_admin` を使う。

## 管理画面（営業リストとして使える）

`?admin=1`（＋スタンドアロンなら `&key=`）で、投稿の一覧が見られる:
日時・投稿者（𝕏 or ドメイン）・タイトル・会社URL・**メールアドレス（公開されない・管理者のみ）**。
投稿者は「自社ドメインのメールを持つ、その会社の本人」なので、そのまま見込み客リストとして使えます。

## AIエージェントが触ってよい範囲

- **変えてよい**: 配色（CSS変数 `--teal` 系）、ヒーロー文言、バッジ、フッター、1日1回制限の時間（`22 * 3600`）、
  タイトル/本文の文字数上限、ページあたり件数（`$per`）、OGP/メタタグ、Google Analytics ID（不要なら該当scriptタグ削除）
- **慎重に**: `kbbs_domain_ok()`（この掲示板の信頼性の核。緩めるとスパム板になる）
- **変えない方がよい**: 投稿データ先頭行の `<?php exit; ?>` ガード、メール非公開の方針、CSRFチェック

## よくあるカスタマイズ例

- 「返信機能をつけて」→ 投稿に `parent_id` を追加し、一覧で親子表示
- 「カテゴリ分けして」→ 投稿フォームに `<select name="category">` を追加し、保存・絞り込み表示
- 「NGワードを入れて」→ 投稿処理のバリデーション連鎖に `mb_strpos` チェックを1つ追加
- 「通知がほしい」→ `kbbs_append()` 直後に `mb_send_mail()` で管理者へ通知

## ライセンス

MIT（LICENSE参照）。改変・再配布・商用利用自由です。
